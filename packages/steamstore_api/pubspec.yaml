name: steamstore_api
description: "A Dart API client for the steam store API."
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  json_annotation: ^4.9.0
  http: ^1.3.0

dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  mocktail: ^1.0.4
  test: ^1.25.15
